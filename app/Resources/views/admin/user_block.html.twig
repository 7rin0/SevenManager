{% extends 'SonataAdminBundle:Core:user_block.html.twig' %}

{% block user_block %}
    {{ locale_switcher(null, null, 'admin/switcher_links.html.twig') }}
    {% if app.user %}
        {{ app.user }}
    - Welcome {{ app.user }} <a href="{{ path('fos_user_security_logout') }}">Logout</a>
    {% endif %}
    {{ parent() }}
{% endblock %}